
<%= current_user.email %>

<%= simple_form_for [@trainer, @booking] do |f| %>
<%= f.text_field :hours, label: 'Enter number of hours needed', error: 'You need to specify the length of the session'%>
<%= f.input :time_of_booking, as: :date %>
<%= f.input :time_of_booking, as: :time %>
<%= f.button :submit, class: "btn btn-primary" %>

<% end %>






